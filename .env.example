DB_DIR=
# Required in production. Generate a unique 32-byte-or-longer secret; never commit it.
JWT_SECRET=replace-with-a-unique-production-secret
# Optional: server-side Gemini key. Do not expose this value through VITE_ variables.
GEMINI_API_KEY=
VITE_API_URL=
